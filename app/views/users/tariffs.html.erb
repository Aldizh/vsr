<h4>List of your agents ... </h4>
<table class = "list">
  <thead>
    <tr>
      <th> Tariff ID </th>
      <th> Tariff Name </th>
      <th>  </td>
    </tr>
  </thead>
  <tbody>
  <% @tariffs.each do |t| %>
    <tr>
      <td><%= t.first[:id_tariff] %> </td>
      <td><%= t.first[:description] %></td>

      <td><%= link_to "View or Edit Tariff", :tariff => t.first, :action => "addRatesToTariff" %></td>
    </tr>
  <% end %>
  </tbody>
</table>